id = "bin"
description = "Personal command-line scripts"

[targets.d_bin]
source = "files/bin"
path = "~/bin"

# askpass-gui only runs on Linux; avoid pulling zenity and GTK stack onto macOS.
[hooks]
pre_push = [
  'if [ "{{ os }}" = "linux" ]; then {{ INSTALL }} zenity; fi',
]
