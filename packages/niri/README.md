# Niri

This package owns the Niri compositor configuration and session integration.
The custom Arch build is managed separately by the dependent
[`niri-custom-git`](../niri-custom-git/README.md) package, allowing Topgrade to
update the executable without pushing this configuration package.
