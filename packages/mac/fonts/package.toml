id = "mac/fonts"
description = "macOS font package"

[targets.mac_fonts_installed]
sync_policy = "push-only"
probe = '{{ PROBE_PACKAGES_INSTALLED }} font-caskaydia-cove-nerd-font'

[targets.mac_fonts_installed.hooks]
pre_push = "{{ INSTALL }} font-caskaydia-cove-nerd-font"
