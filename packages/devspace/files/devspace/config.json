{
  "configVersion": 1,
  "publicBaseUrl": {% if vars.devspace.public_base_url %}"{{ vars.devspace.public_base_url }}"{% else %}null{% endif %},
  "allowedRoots": ["~/Projects"],
  "agentDir": "~/.agents"
}
