# Use the built-in darker Gruvbox palette.
theme = "gruvbox-dark-hard"

# Keep the managed config stable instead of prompting Hunk to write view changes.
prompt_save_view_preferences = false

# Wrap long diff lines instead of clipping them horizontally.
wrap_lines = true
