id = "cliamp"
description = "Terminal music player configuration"

[targets.f_config_cliamp_config_toml]
source = "files/config/cliamp/config.toml"
path = "~/.config/cliamp/config.toml"
chmod = "600"

[hooks]
pre_push = [
  "{{ INSTALL }} cliamp-bin",
]
